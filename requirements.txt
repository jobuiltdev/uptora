celery[redis]==5.6.3
Django==6.1
gunicorn==23.0.0
httpx==0.28.1
djangorestframework==3.18.0
djangorestframework-simplejwt==5.5.1
django-storages[s3]==1.14.6
playwright==1.62.0
psycopg[binary]==3.3.4
python-dotenv==1.2.3
whitenoise==6.12.0

# Development / tooling
pytest==9.1.1
pytest-django==4.14.0
ruff==0.16.5

# Browser monitoring needs the Chromium build too, which pip does not
# install. After pip install, run:
#     playwright install chromium
#
# Background scheduling needs a Redis broker. See docs/scheduling.md for
# local setup and worker commands.
